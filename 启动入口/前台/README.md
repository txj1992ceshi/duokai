# 前台启动入口

这里放的是 **Duokai Web 前端** 的标准入口。

优先使用：

- `前台_日常启动_Mac.command`
- `前台_日常启动_Windows.bat`
- `前台_首次安装并启动_Mac.command`
- `前台_首次安装并启动_Windows.bat`

默认打开：

- `http://localhost:3001`

不要再使用根目录或 `legacy-launchers` 里的旧入口文件。
