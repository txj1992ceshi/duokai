# 后台启动入口

这里放的是 **管理后台** 的标准入口。

优先使用：

- `后台_日常启动_Mac.command`
- `后台_日常启动_Windows.bat`
- `后台_首次安装并启动_Mac.command`
- `后台_首次安装并启动_Windows.bat`

默认打开：

- `http://localhost:3000`

不要再使用根目录或 `legacy-launchers` 里的旧入口文件。
